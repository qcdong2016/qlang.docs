# 10. 赋值与运算符


```
package demo

func f() {
    x := 1
    x = 2
    x += 1
    x -= 1
    x *= 2
    x /= 2
    x %= 2
    x &= 1
    x |= 2
    x ^= 3
    x <<= 1
    x >>= 1
    x++
    x--
    ++x
    --x
}
```

```
package demo

func f() {
    a := 1 + 2
    b := 3 - 1
    c := 2 * 3
    d := 7 / 2
    e := 7 % 2
    f := 1 == 1
    g := 1 != 2
    h := 1 < 2
    i := 1 <= 2
    j := 2 > 1
    k := 2 >= 1
    m := true && false
    n := true || false
    p := !true
    q := 1 & 3
    r := 1 | 2
    s := 1 ^ 3
    t := ^1
    u := 1 << 2
    v := 8 >> 1
    w := 1 &^ 1
}
```

优先级从高到低：`++ --`（后缀） → `+ - ! ^ * & ++ --`（前缀） → `* / % << >> & &^` → `+ - | ^` → `== != < <= > >=` → `&&` → `||`。

---
