# 11. 类型转换


```
package demo

func f() {
    var x int = 1
    var y float = float(x)
    var z i32 = i32(x)
    var s string = string([]u8{'a', 'b'})
    var b []u8 = []u8(s)
}
```

指针转换必须加括号：

```
package demo

struct Point { x, y int };

func f() {
    var p *Point
    var q *byte = (*byte)(p)
    var r *Point = (*Point)(q)
}
```

```
package demo

struct Node : Ref { v int };

func f(n &Node) {
    var p *Node = &*n
    var q &Node = (&Node)(p)   // 非法：*T 不能转成 &T
}
```

---
