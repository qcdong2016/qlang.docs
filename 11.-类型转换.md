# 11. 类型转换


```
package demo

func f() {
    var x int = 1
    var y float = float(x)
    var z i32 = i32(x)
    var s string = string([]u8{'a', 'b'})
    var b []u8 = []u8(s)
}
```

接口 / `any` 取出具体类型：先 `typeof(x) == T`，再用 `T(x)`。

```
package demo

struct Node : Ref { v int };

func f(x any) {
    if typeof(x) == Node {
        n := Node(x)
        _ = n
    }
}
```

---
