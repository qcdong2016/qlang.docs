# 12. 结构体


### 12.1 值结构体

```
package demo

struct Point {
    x int
    y int
};

struct Rect {
    a, b Point
};

func f() {
    p := Point{}
    q := Point{1, 2}
    r := Point{x: 1, y: 2}
    s := Point{x: 1}
    t := Rect{a: Point{1, 0}, b: Point{2, 3}}
    u := p
    u.x = 9
    // p.x 仍为 1
}
```

无 `&` 类型前缀。是否 RC 由 struct 声明上的 `: Ref` 决定。

### 12.2 字段

```
package demo

struct Point { x, y int };

func f() {
    p := Point{x: 1, y: 2}
    p.x = 3
    a := p.x
}
```

类型名上的字段是成员引用，类型 `StructField`，见「类型值与成员引用」。

```
package demo

struct Point { x, y int };

func f() {
    var m StructField = Point.x
    _ = m
}
```

### 12.3 Ref 结构体

`struct T : Ref` 堆分配，引用计数；类型名是 `T`，无 `&` 前缀。

```
package demo

struct Node : Ref {
    next Node
    v    int
};

func f() {
    n := Node{}
    m := Node{v: 1}
    p := Node{next: m, v: 2}
    q := n
    // q、n 指向同一对象，赋值 retain
    n.v = 3
    // q.v == 3
}
```

嵌套：

```
package demo

struct Edge : Ref {
    to   Node
    cost int
};

struct Node : Ref {
    next Node
    e    Edge
    v    int
};
```

### 12.4 类型参数

写在结构体名后。约束是接口。成员函数不再写类型参数，直接用 `T`。

```
package demo

interface Writer {
    Write(buf []u8) int
};

struct File {
    fd int
};

func File.Write(buf []u8) int { return 0 }

struct Box<T Writer> {
    v T
};

func Box.Get() T {
    return this.v
}

func Box.Set(T a) {
    this.v = a
}

func Box.Dump() {
    this.v.Write(nil)
}

func f() {
    var b Box<File>
    b.Set(File{fd: 1})
    x := b.Get()
    b.Dump()
}
```

```
package demo

interface Writer {
    Write(buf []u8) int
};

struct Box<T Writer> { v T };

func Box<T>.Set(T a) {}     // 非法：成员不再声明 T
func Box.Set<T>(T a) {}     // 非法
func Box.Set(U a) {}        // 非法：U 未在结构体上声明
```

可与 `: Ref` 同时写。

```
package demo

interface Writer {
    Write(buf []u8) int
};

struct Box<T Writer> : Ref {
    v T
};

func Box.Set(T a) {
    this.v = a
}

func f(f File) {
    n := Box<File>{v: f}
    n.Set(f)
}
```

多个参数：

```
package demo

interface Eq {
    Equal(other any) bool
};

struct Pair<A Eq, B Eq> {
    a A
    b B
};

func Pair.Left() A { return this.a }
func Pair.Right() B { return this.b }
```

`this` 类型是 `T`，不写在参数表。调用仍是 `x.Func(...)`。

---
