# 13. 裸指针


同 C++。不参与 RC。不延长生命周期。

### 13.1 取址、解引用、成员

```
package demo

struct Point { x, y int };

func f() {
    p := Point{x: 1, y: 2}
    q := &p          // *Point
    x := *q          // Point
    q->x = 3
    a := q->y
    b := (*q).x
    r := &p.x        // *int
    *r = 4
}
```

### 13.2 算术与下标

```
package demo

func f() {
    var buf [4]int
    p := &buf[0]     // *int
    p = p + 1
    p = p - 1
    p++
    p--
    n := p - &buf[0] // int
    v := p[0]
    p[1] = 9
    q := &p[2]
}
```

步长为 `sizeof(T)`。

### 13.3 new / free

`new` 返回 `*T`，不是 `&T`。必须 `free`。

```
package demo

struct Point { x, y int };

func f() {
    p := new(Point)
    p->x = 1
    q := new(Point{x: 2, y: 3})
    free(p)
    free(q)
}
```

```
package demo

struct Node : Ref { v int };

func f() {
    n := &Node{v: 1}
    p := &*n         // *Node，不 +1
    p->v = 2
    // 不要 free(p)：载荷由 RC 释放
}
```

### 13.4 nil 与比较

```
package demo

struct Point { x int };

func f() {
    var p *Point = nil
    if p == nil {}
    q := new(Point)
    if p != q {}
    free(q)
}
```

---
