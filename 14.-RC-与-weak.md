# 14. RC 与 weak


### 14.1 赋值与作用域

```
package demo

struct Node : Ref { v int };

func f() {
    a := Node{v: 1}
    b := a
    a = Node{v: 2}
    // 原对象若无其它引用则释放
}
```

### 14.2 传参与返回

默认值传递。Ref 结构体默认值传递时 `retain`。

```
package demo

struct Point { x int };
struct Node : Ref { v int };

func take_val(p Point) {
    p.x = 9
}

func take_rc(n Node) {
    n.v = 9
}

func make_node() Node {
    return Node{v: 1}
}

func f() {
    p := Point{x: 1}
    take_val(p)
    // p.x == 1
    n := make_node()
    take_rc(n)
    // n.v == 9
}
```

### 14.3 weak

```
package demo

struct Node : Ref {
    parent weak Node
    child  Node
    v      int
};

func f() {
    a := Node{v: 1}
    b := Node{parent: weak(a), v: 2}
    a.child = b
    p, ok := b.parent.upgrade()
    if ok {
        p.v = 3
    }
}
```

```
package demo

struct Node : Ref { v int };

func f() {
    w := weak(Node{v: 1})
    // 临时对象无其它引用，随后 upgrade 失败
    p, ok := w.upgrade()
    // ok == false, p == nil
}
```

`weak(x)` 的 `x` 必须是 Ref 结构体值。

```
package demo

struct Point { x int };

func f() {
    p := Point{}
    _ = weak(p)       // 非法：Point 不是 : Ref
}
```

---
