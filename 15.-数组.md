# 15. 数组


值类型。长度是类型的一部分。

`[]T{...}` 按左侧类型解释；左侧无类型时是数组。

```
package demo

func f() {
    var a [3]int = []int{1, 2, 3}
    b := []int{1, 2, 3}
    c := [3]int{1}
    a[0] = 1
    x := a[1]
    n := len(a)
}
```

```
package demo

func f() {
    var a [3]int = []int{1, 2, 3}
    d := []int{1, 2, 3}
    // a、d 都是 [3]int
    var s []int = []int{1, 2, 3}
    // s 是 []int
}
```

```
package demo

func f() {
    var a [3]int
    var b [4]int
    a = b            // 非法：长度不同
}
```

数组按值拷贝：

```
package demo

func f() {
    a := [2]int{1, 2}
    b := a
    b[0] = 9
    // a[0] == 1
}
```

---
