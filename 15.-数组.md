# 15. 数组


值类型。长度是类型的一部分。

数组字面量用 `[n]T{...}` 或 `[...]T{...}`。`[...]` 仅出现在复合字面量里，长度由元素个数（或最大下标 + 1）在编译期确定。

`[]T{...}` 永远是切片，不是数组。

```
package demo

func f() {
    var a [3]int = [3]int{1, 2, 3}
    b := [...]int{1, 2, 3}
    c := [3]int{1}
    a[0] = 1
    x := a[1]
    n := len(a)
    // b 类型是 [3]int
}
```

```
package demo

func f() {
    d := []int{1, 2, 3}
    // d 是 []int
    var a [3]int = [...]int{1, 2, 3}
    var s []int = []int{1, 2, 3}
    a = [3]int{1, 2, 3}
    // a 是 [3]int，s 是 []int
}
```

```
package demo

func f() {
    var a [3]int
    var b [4]int
    a = b            // 非法：长度不同
    _ = [...]int     // 非法：缺 {}
    var c [...]int   // 非法：声明不能写 [...]
    var d [3]int = []int{1, 2, 3}   // 非法：[]int 不是 [3]int
}
```

带下标时，长度取最大下标 + 1，未写位置为零值：

```
package demo

func f() {
    a := [...]int{1: 10, 3: 30}
    // a 类型是 [4]int，值为 {0, 10, 0, 30}
}
```

数组按值拷贝：

```
package demo

func f() {
    a := [2]int{1, 2}
    b := a
    b[0] = 9
    // a[0] == 1
}
```

---
