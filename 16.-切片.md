# 16. 切片


`[]T` 是 RC。底层数组共享。

字面量 `[]T{...}` 永远是切片。数组只用 `[n]T{...}` 或 `[...]T{...}`，见「数组」。

```
package demo

func f() {
    var s []int = []int{1, 2, 3}
    a := []int{1, 2, 3}
    // a 是 []int
    b := make([]int, 3)
    c := make([]int, 3, 8)
    n := len(s)
    m := cap(s)
    s[0] = 9
    d := s[1:3]
    e := s[:2]
    g := s[1:]
    h := s[:]
    i := append(s, 4)
    var extra []int = []int{5, 6}
    j := append(s, extra...)
}
```

从数组切：

```
package demo

func f() {
    a := [4]int{1, 2, 3, 4}
    s := a[:]
    s[0] = 9
    // a[0] == 9
}
```

---
