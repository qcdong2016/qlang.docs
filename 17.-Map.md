# 17. Map


`Map<K, V>` 是 RC。`K` 必须可比较（无 `[]T`、`Map`、`func`、`&T`）。

```
package demo

func f() {
    var m Map<string, int>
    m = make(Map<string, int>)
    n := Map<string, int>{"a": 1, "b": 2}
    n["c"] = 3
    x := n["a"]
    y, ok := n["z"]
    delete(n, "a")
    k := len(n)
}
```

```
package demo

struct Node : Ref { v int };

func f() {
    var m Map<&Node, int>   // 非法：&T 不可作 key
    var n Map<*int, int>    // 可以：裸指针可比较
}
```

---
