# 17. Map


`Map<K, V>` 是预声明普通结构体（`: Ref`）。`K` 须实现 `Eq`。

读写可用下标；删除用成员函数 `Delete`。

```
package demo

func f() {
    var m Map<string, int>
    m = make(Map<string, int>)
    n := Map<string, int>{"a": 1, "b": 2}
    n["c"] = 3
    x := n["a"]
    y, ok := n["z"]
    n.Delete("a")
    k := len(n)
}
```

```
package demo

struct Node : Ref { v int };

func f() {
    var m Map<Node, int>   // 非法：Node 未实现 Eq
}
```

```
package demo

struct Key {
    id int
};

func Key.Equal(other any) bool {
    if typeof(other) != Key {
        return false
    }
    k := Key(other)
    return this.id == k.id
}

func f() {
    m := make(Map<Key, int>)
    m[Key{id: 1}] = 10
    m.Delete(Key{id: 1})
}
```

---
