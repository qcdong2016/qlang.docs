# 17. Map


`Map<K, V>` 是 RC。`K` 必须可比较（无 `[]T`、`Map`、`func`、Ref 结构体）。

```
package demo

func f() {
    var m Map<string, int>
    m = make(Map<string, int>)
    n := Map<string, int>{"a": 1, "b": 2}
    n["c"] = 3
    x := n["a"]
    y, ok := n["z"]
    delete(n, "a")
    k := len(n)
}
```

```
package demo

struct Node : Ref { v int };

func f() {
    var m Map<Node, int>   // 非法：Ref 结构体不可作 key
}
```

---
