# 18. 字符串


`string` 是 RC。内容不可改；变量可重新赋值。

```
package demo

func f() {
    s := "ab"
    t := s + "c"
    n := len(s)
    c := s[0]
    u := s[1:]
    v := s[:1]
    w := s[0:2]
    b := []u8(s)
    x := string(b)
}
```

```
package demo

func f() {
    s := "ab"
    s[0] = 'x'       // 非法
}
```

---
