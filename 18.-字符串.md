# 18. 字符串


`string` 是 RC。内容 UTF-8，不可改；变量可重新赋值。

`len(s)`、`s[i]`、切片按下标按字节。`s[i]` 类型为 `u8`。`range` 按 Unicode 码点：`i` 为字节下标，`c` 为 `int`（码点）。

```
package demo

func f() {
    s := "ab"
    t := s + "c"
    n := len(s)
    c := s[0]
    u := s[1:]
    v := s[:1]
    w := s[0:2]
    b := []u8(s)
    x := string(b)
}
```

```
package demo

func f() {
    s := "ab"
    s[0] = 'x'       // 非法
}
```

```
package demo

func f() {
    s := "你好"
    n := len(s)           // 字节数
    for i, c := range s {
        _ = i
        _ = c
    }
}
```

---
