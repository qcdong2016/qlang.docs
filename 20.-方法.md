# 20. 方法


声明：`func Class.Func(...)`。体内用 `this`，类型为 `T`（编译器插入，不写在参数表）。

```
package demo

struct Point { x, y float };

struct Node : Ref { v int };

func Point.Len2() float {
    return this.x*this.x + this.y*this.y
}

func Point.Scale(k float) {
    this.x *= k
    this.y *= k
}

func Node.Val() int {
    return this.v
}

func Node.Set(v int) {
    this.v = v
}

func f() {
    p := Point{x: 3, y: 4}
    a := p.Len2()
    p.Scale(2)
    n := Node{v: 1}
    b := n.Val()
    n.Set(2)
}
```

值结构体的可变方法：编译器隐式传址，不引入裸指针类型。

调用方写 `x.Func(...)`。值、Ref 结构体都可以调。

```
package demo

struct Point { x int };
struct Node : Ref { v int };

func Point.A() {}
func Node.C() {}

func f() {
    var v Point
    var n Node = Node{}
    v.A()
    n.C()
    v.C()            // 非法：Point 没有 C
}
```

`this` 不能另声明，不能当 RC。方法里对 `this` 不 `retain`。

```
package demo

struct Point { x int };

func Point.F() {
    var this Point   // 非法
    this = Point{}   // 非法：不能赋 this
}
```

方法值、方法表达式：

```
package demo

struct Point { x int };

func Point.Set(x int) { this.x = x }

func f() {
    p := Point{x: 1}
    f1 := p.Set
    f1(2)
    f2 := Point.Set
    f2(p, 3)
}
```

类型参数只写在结构体上。方法里直接用 `T`。

```
package demo

interface Reader {
    Read(buf []u8) (int, error)
};

struct File { fd int };

func File.Read(buf []u8) (int, error) { return (0, nil) }

struct Slot<T Reader> {
    v T
};

func Slot.Put(T a) {
    this.v = a
}

func Slot.Get() T {
    return this.v
}

func f() {
    var s Slot<File>
    s.Put(File{fd: 1})
    x := s.Get()
}
```

```
package demo

struct Slot<T Reader> { v T };

func Slot<T Reader>.Put(T a) {}  // 非法
```
