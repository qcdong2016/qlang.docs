# 23. 内建


```
package demo

struct Point { x int };
struct Node : Ref { v int };

func f() {
    s := make([]int, 2, 4)
    m := make(Map<int, int>)
    n := len(s)
    c := cap(s)
    s = append(s, 1)
    delete(m, 1)
    z := sizeof(Point)
    z = sizeof(n)
    w := weak(Node{v: 1})
    q, ok := w.upgrade()
    _ = q
    _ = ok
}
```

```
package demo

func f() {
    panic("boom")
}

func g() {
    defer func() {
        if e := recover(); e != nil {
        }
    }()
    panic(1)
}
```

`sizeof(T)` / `sizeof(expr)` 得到 `uint`。对 Ref 结构体是句柄宽度，不含载荷。

---
