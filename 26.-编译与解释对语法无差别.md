# 26. 编译与解释（对语法无差别）


```
package demo

var Compile []string = []string{"math"}
```

```
package math

func Add(a int, b int) int {
    return a + b
}

struct Counter : Ref {
    n int
};

func Counter.Inc() {
    this.n++
}

interface Inc {
    Inc()
};
```

```
package main

import "math"

func main() {
    x := math.Add(1, 2)
    c := math.Counter{n: 0}
    var i math.Inc = c
    i.Inc()
}
```

`math` 已编译、`main` 解释时，上面调用仍合法：`Add` 走原生符号，`Inc` 走 itab。源码写法不变。

---
