# 27. 类型值与成员引用


类型名、结构体字段名可作为值：当参数、变量。

### 27.1 类型值

类型名写作表达式时，值的类型是 `Type`。

`Type` 是预声明名。出现在类型位置时表示元类型；出现在声明头 `type Name T` 时仍是关键字。

```
package demo

struct A {
    x int
    y string
};

type Count int

func take_type(t Type) Type {
    return t
}

func f() {
    var t Type = A
    t = Count
    t = int
    t = []u8
    t = Map<string, int>
    u := take_type(A)
    _ = u
}
```

预声明类型、命名类型、`struct`、`interface`、RC、切片、数组、`Map`、函数、元组均可作为 `Type` 值。

```
package demo

func f() {
    var t Type = 1        // 非法：不是类型名
    var a A = A           // 非法：右侧是 Type，不是 A
}
```

### 27.2 成员引用

`StructName.Field` 在 `StructName` 为类型名、`Field` 为该结构体字段时，是成员引用。值的类型是 `StructField`（struct field）。

```
package demo

struct A {
    x int
    y string
};

func take_field(m StructField) StructField {
    return m
}

func f() {
    var m StructField = A.x
    m = A.y
    n := take_field(A.x)
    _ = n
}
```

仅结构体（含 `struct T : Ref`）的字段可写成成员引用。

```
package demo

struct A { x int };
type Count int
interface Reader { Read() int };

func f() {
    var m StructField = A.x
    _ = Count.x           // 非法：不是结构体
    _ = Reader.Read       // 非法：不是字段；方法见「方法」
    _ = int.x             // 非法
}
```

与实例字段、方法表达式区分：左侧是类型名且右侧是字段 → `StructField`；左侧是值 → 普通字段访问；右侧是方法 → 方法表达式。

```
package demo

struct A { x int };

func A.Get() int { return this.x }

func f() {
    var t Type = A
    var m StructField = A.x
    a := A{x: 1}
    v := a.x              // int
    f1 := A.Get           // 方法表达式
    _ = t
    _ = m
    _ = v
    _ = f1
}
```

### 27.3 用法

可作参数、返回值、变量、赋值左右边。`Type` 与 `StructField` 只能与同种比较。

```
package demo

struct A {
    x int
    y string
};

struct B {
    x int
};

func same_type(a Type, b Type) bool {
    return a == b
}

func same_field(a StructField, b StructField) bool {
    return a == b
}

func f() {
    var t Type = A
    var m StructField = A.x
    t = B
    m = B.x
    _ = same_type(A, B)
    _ = same_field(A.x, A.y)
    _ = same_field(A.x, B.x)
}
```

```
package demo

struct A { x int };

func f() {
    var t Type = A
    var m StructField = A.x
    t = A.x               // 非法
    m = A                 // 非法
    _ = t == A.x          // 非法
}
```

---
