# 4. 标识符


```
package demo

var x int
var _x int
var foo_bar int
var 点数 int
```

```
var 1x int     // 非法
var func int     // 非法：关键字
var this int   // 非法：this 保留给成员函数
```

`_` 是空白标识符，赋值丢弃。

```
_, err := open("a.q")
```

---
