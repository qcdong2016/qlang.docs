# 5. 字面量


### 5.1 整数

```
package demo

func f() {
    a := 42
    b := 0xFF
    c := 0xff
    d := 0b1010
    e := 0o755
    g := 1_000_000
}
```

### 5.2 浮点

```
package demo

func f() {
    a := 3.14
    b := 1e6
    c := .5
    d := 1.0
}
```

### 5.3 布尔

```
package demo

func f() {
    a := true
    b := false
}
```

### 5.4 字符串

```
package demo

func f() {
    a := "hello"
    b := "a\nb\t\"c\\"
    c := `原始字符串
可换行，不转义`
}
```

### 5.5 字符

`char` 是 `u8`。单引号一个字节。

```
package demo

func f() {
    a := 'A'
    b := '\n'
    c := '\x41'
}
```

### 5.6 nil

`nil` 可用于 Ref 结构体、`[]T`、`Map<K, V>`、`func`、`interface`、`weak T`（T 为 : Ref）。

```
package demo

func f() {
    var n Node = nil
    var s []int = nil
    var m Map<string, int> = nil
    var f func() = nil
    var r Reader = nil
}
```

---
