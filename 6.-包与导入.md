# 6. 包与导入


项目根目录放 `qlang.q`。`package` 就是包名。

```
package demo

var Compile []string = []string{"math"}
```

没有 `Compile` 时只编译标准库，其余解释。

```
package app

import "math"
import "std/io"

func main() {
    x := math.add(1, 2)
    io.println(x)
}
```

分组：

```
package app

import (
    "math"
    "std/io"
)
```

别名：

```
package app

import m "math"
import . "std/io"

func main() {
    x := m.add(1, 2)
    println(x)
}
```

解释执行时：`import "math"` 若存在 `math.qso` 则加载编译包，否则解释 `math` 源码。调用写法相同。

```
package app

import "math"

func main() {
    math.add(1, 2)
}
```

---
