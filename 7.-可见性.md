# 7. 可见性


同 Go：首字母大写导出，小写仅本包可见。

```
package math

func Add(a int, b int) int {
    return a + b
}

func clamp(x int) int {
    return x
}

struct Point {
    X int
    y int
};

struct Node : Ref {
    Next Node
    v    int
};
```

```
package app

import "math"

func main() {
    math.Add(1, 2)
    math.clamp(1)               // 非法：未导出
    p := math.Point{X: 1, y: 2} // 非法：y 未导出
    p.X = 3
    p.y = 4                     // 非法
}
```

---
