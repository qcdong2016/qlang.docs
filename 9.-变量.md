# 9. 变量


```
package demo

var g int
var h int = 1
var i = 2
var j, k int
var m, n = 1, 2
var (
    a int
    b string = "x"
)

func f() {
    var x int
    var y int = 1
    z := 2
    p, q := 3, 4
    p, q = q, p
}
```

短声明至少有一个新变量：

```
package demo

func f() {
    x := 1
    x, y := 2, 3
    x := 4        // 非法：没有新变量
}
```

零值：数值 `0`，`bool` 为 `false`，`string` 为 `""`，Ref 结构体、`[]T`、`Map`、`func`、`interface` 为 `nil`，值结构体字段全零。

```
package demo

struct Point { x, y int };
struct Node : Ref { v int };

func f() {
    var p Point
    // p.x == 0, p.y == 0
    var n Node
    // n == nil
}
```

---
