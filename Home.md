# QLang 语法

默认值传递。`&T` 是 RC（`T` 必须是 `struct T : Ref`）。`*T` 是裸指针（同 C++）。接口同 Go。类型名可作值（类型 `type`）；`Struct.Field` 可作值（类型 `StructField`）。

源文件后缀 `.q`。语句无分号。`struct` / `interface` 声明以 `};` 结束。

语法按章节写在左侧目录。
