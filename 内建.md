# 内建

```
s := make([]int, 2, 4)
m := make(Map<int, int>)
n := len(s)
c := cap(s)
s = append(s, 1)
z := sizeof(Point)
t := typeof(n)
w := weak(n)
q, ok := w.upgrade()
```

```
panic("boom")
defer func() {
    if e := recover(); e != nil {
    }
}()
```

`sizeof(T)` / `sizeof(expr)` 得 `uint`。对 Ref 结构体是句柄宽度。

`typeof(expr)` 得 `Type`，等于运行时动态类型。
