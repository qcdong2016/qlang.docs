# 函数

单返回 `: Type`。多返回 `: (Type, Type)`，无名字。

```
func empty() {}

func add(a : int, b : int) : int {
    return a + b
}

func add2(a, b : int) : int {
    return a + b
}

func swap(a : int, b : int) : (int, int) {
    return b, a
}

x := add(1, 2)
a, b := swap(1, 2)
_, c := swap(3, 4)
```

## 可变参数

```
func sum(xs : ...int) : int {
    n := 0
    for i := 0; i < len(xs); i += 1 {
        n += xs[i]
    }
    return n
}

sum(1, 2, 3)
sum(xs...)
```

## 函数值

```
inc := func(x : int) : int { return x + 1 }
n := inc(1)
x := func(a : int) : int { return a + 1 }(3)
```

捕获值：拷贝。捕获 Ref：`retain`。

## 方法

声明 `func Class.Func(...)`。结构体与类型别名均可。体内用 `this`，类型为 `T`，不写在参数表。不对 `this` `retain`。

```
struct Point { x, y : float };

func Point.Len2() : float {
    return this.x*this.x + this.y*this.y
}

func Point.Scale(k : float) {
    this.x *= k
    this.y *= k
}

p := Point{x: 3, y: 4}
a := p.Len2()
p.Scale(2)
```

值结构体可变方法：对可寻址接收者隐式传址，改的是调用方变量。调用一律 `x.Func(...)`。

方法值捕获接收者：值结构体绑变量地址，存活期与方法值相同；临时量绑副本。Ref：`retain`。

```
f1 := p.Set
f1(2)
f2 := Point.Set
f2(p, 3)
```

## 类型参数

使用时显式写实参。

```
func B<T: Writer>() : T {
    return T{}
}

x := B<File>()
```

```
func A.func1<T: Writer>() {
}

a.func1<File>()
```

## 运算符重载

`func T.op(...)`。`this` 是左操作数（前缀则是唯一操作数）。

可重载：`+ - * / %` `[]` `[]=` `& | ^ &^ << >>` `== != < <= > >=` 以及前缀 `+ - ! ^`。

不可重载：`=` `:=` `&&` `||` `.` 调用 `()`。

```
func Point.+(b : Point) : Point {
    return Point{x: this.x + b.x, y: this.y + b.y}
}

c := a + b
```

```
func Vec.[](i : int) : int {
    return this.data[i]
}

func Vec.[]=(i : int, v : int) {
    this.data[i] = v
}

x := v[0]
v[0] = 1
```
