# 包

全静态编译。同一目录、同一 `package` 名的多个 `.q` 组成一包。无 `init`。包级变量按文件名再按声明顺序初始化。无循环导入。

导入路径相对项目根；`std/` 为标准库。

```
package app

import "math"
import "std/io"

func main() {
    x := math.Add(1, 2)
    io.println(x)
}
```

```
import (
    "math"
    "std/io"
)

import m "math"
import . "std/io"
```

## 可见性

首字母大写导出，小写仅本包可见。

```
package math

func Add(a : int, b : int) : int { return a + b }
func clamp(x : int) : int { return x }
```

```
package app

import "math"

func main() {
    math.Add(1, 2)
}
```

## 入口

可执行包必须是 `package main`，且有 `func main()`。两种签名二选一，无返回值。

```
package main

func main() {}
```

```
package main

func main(args : []string) {}
```
