# 变量

```
let g : int
let h : int = 1
let i = 2
let j, k : int
let m, n = 1, 2
let (
    a : int
    b : string = "x"
)

x := 1
p, q := 3, 4
p, q = q, p
```

短声明至少有一个新变量。

零值：数值 `0`，`bool` 为 `false`，`string` 为 `""`，Ref / `[]T` / `Map` / `func` / `interface` 为 `nil`，值结构体字段全零。

## 赋值

`=` `:=` 与复合赋值是语句，不是表达式。

```
x = 2
x += 1
x -= 1
x *= 2
x /= 2
x %= 2
x &= 1
x |= 2
x ^= 3
x <<= 1
x >>= 1
x &^= 1
```

## 运算符

```
a + b
a - b
a * b
a / b
a % b
a == b
a != b
a < b
a && b
a || b
!a
a & b
a | b
a ^ b
^a
a << 2
a >> 1
a &^ b
```

优先级从高到低，同级左结合（前缀右结合）：

| 级 | 算符 | 结合 |
| --- | --- | --- |
| 1 | `.` `[]` `()` 调用 `{}` 字面量 类型实参 `<...>` | 左 |
| 2 | `+` `-` `!` `^`（前缀） | 右 |
| 3 | `*` `/` `%` `<<` `>>` `&` `&^` | 左 |
| 4 | `+` `-` `\|` `^` | 左 |
| 5 | `==` `!=` `<` `<=` `>` `>=` | 左 |
| 6 | `&&` | 左 |
| 7 | `\|\|` | 左 |

`==` / `!=` 要求实现 `Eq`。`<` `<=` `>` `>=` 仅用于数值与 `string`。自定义类型可重载部分运算符，见「函数」。
