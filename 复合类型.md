# 复合类型

## 数组

值类型。长度是类型的一部分。字面量 `[n]T{...}` 或 `[...]T{...}`。`[]T{...}` 永远是切片。

```
let a : [3]int = [3]int{1, 2, 3}
b := [...]int{1, 2, 3}
c := [3]int{1}
a[0] = 1
n := len(a)
```

`[...]` 仅出现在字面量里。带下标时长度取最大下标 + 1。赋值按值拷贝。

## 切片

`[]T` 是 RC。底层数组共享。

```
s := []int{1, 2, 3}
b := make([]int, 3)
c := make([]int, 3, 8)
n := len(s)
m := cap(s)
d := s[1:3]
e := s[:2]
i := append(s, 4)
j := append(s, []int{5, 6}...)
```

```
a := [4]int{1, 2, 3, 4}
s := a[:]
```

## Map

`Map<K, V>` 是预声明 `: Ref` 结构体。`K` 须实现 `Eq`。删键用 `Delete`。

```
m := make(Map<string, int>)
n := Map<string, int>{"a": 1, "b": 2}
n["c"] = 3
x := n["a"]
y, ok := n["z"]
n.Delete("a")
k := len(n)
```

## 字符串

`string` 是 RC，UTF-8，内容不可改。`len`、下标、切片按字节。`s[i]` 类型是 `uint8`。`range` 按码点，`i` 为字节下标，`c` 为 `int`。

```
s := "ab"
t := s + "c"
n := len(s)
c := s[0]
u := s[1:]
b := []uint8(s)
x := string(b)
```
