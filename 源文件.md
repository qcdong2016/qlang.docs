# 源文件

后缀 `.q`。一个文件一个 `package`，必须写在第一行（注释除外）。语句无分号，换行结束规则见「附录-词法」。`struct` / `interface` 以 `};` 结束。

```
package math

func add(a : int, b : int) : int {
    return a + b
}
```

```
// 行注释
x := 1 // 行尾
/*
    块注释
    不可嵌套
*/
```
