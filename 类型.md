# 类型

类型写在 `:` 后：`let a : Type`、`func f(a : Type) : Type`、`struct S { A : Type }`。

## 预声明

```
bool
char
int8 int16 int32 int64
uint8 uint16 uint32 uint64
int uint                // 机器字宽
float
f32 f64
string
error
any
Eq
Type
StructField
```

```
let a : int = 1
let b : uint8 = 255
let c : float = 1.5
let d : bool = true
let e : string = "hi"
let f : any = 1
```

`char` 与 `uint8`、`float` 与 `f64` 不是同一类型，转换须显式：`uint8(c)`、`float(x)`、`f64(y)`。

## 类型别名

`type Name = T`。新类型，底层为 `T`，可写方法。与 `T` 互转须显式，同 Go 的定义类型。

```
type Count = int
type Name = string

let n : Count = Count(1)
let i : int = int(n)

func Count.Inc() : Count {
    return Count(int(this) + 1)
}
```

## 复合

```
struct Point { x, y : int };
struct Node : Ref { v : int };

let a : Point
let c : Node
let d : [4]int
let e : []int
let g : Map<string, int>
let h : func(x : int) : int
let w : weak Node
```

`: Ref` 只能写在 `struct` 上。类型名无 `&` 前缀。函数可多返回值，无元组。

`<...>` 在类型位置、调用/字面量前是类型实参；比较仍是 `a < b`。

类型参数 `<T: Interface>`，结构体、函数、方法可写。使用时显式指定：`A<SomeType>{}`、`B<SomeType>()`。约束必须是接口名。无泛型接口。

```
func swap(a : int, b : int) : (int, int) {
    return b, a
}

a, b := swap(1, 2)
```

## 可比

`==` / `!=` 要求实现 `Eq`。Map key 须实现 `Eq`。预声明数值、`bool`、`string`、`Type`、`StructField` 已实现。

| 类型 | `==` `!=` | Map key |
| --- | --- | --- |
| bool、整数、char、string | 是 | 是 |
| float / f32 / f64 | 是（NaN != NaN） | 是 |
| 值结构体、数组 | 字段/元素均可比较 | 同左 |
| Ref 结构体 | 比句柄，可与 `nil` | 否 |
| `[]T`、`Map`、`func` | 仅与 `nil` | 否 |
| interface / `any` | 动态类型相同且可比较，或均为 `nil` | 动态类型可作 key |
| `Type`、`StructField` | 仅同种 | 是 |
| `weak T` | 仅与 `nil` | 否 |

## 转换

```
let x : int = 1
let y : float = float(x)
let z : int32 = int32(x)
let s : string = string([]uint8{'a', 'b'})
let b : []uint8 = []uint8(s)
```

接口 / `any`：先 `typeof(x) == T`，再 `T(x)`。

```
func f(x : any) {
    if typeof(x) == int {
        n := int(x)
        _ = n
    }
}
```

## 类型值

类型名作表达式时类型为 `Type`。

```
let t : Type = int
t = Point
t = []uint8
t = Map<string, int>
_ = typeof(x) == Point
```

## 成员引用

`Struct.Field` 类型为 `StructField`。

```
let m : StructField = Point.x
m = Point.y
```

左侧是值 → 字段访问；右侧是方法 → 方法表达式。
