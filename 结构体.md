# 结构体

无结构体内嵌。循环引用必须用 `weak`，否则泄漏。

## 值结构体

```
struct Point {
    x : int
    y : int
};

p := Point{}
q := Point{1, 2}
r := Point{x: 1, y: 2}
s := Point{x: 1}
u := p
u.x = 9
```

赋值拷贝。无 `&` 类型前缀。

类型名后紧跟 `{` 是复合字面量，不是块；类型与 `{` 不可换行拆开。

## Ref

`struct T : Ref` 堆分配，引用计数；类型名仍是 `T`。

```
struct Node : Ref {
    next : Node
    v    : int
};

n := Node{v: 1}
q := n
n.v = 3
```

`q`、`n` 指向同一对象。凡 Ref 写入另一位置都 `retain`：赋值、传参、返回、进接口、值结构体拷贝其 Ref 字段。

可与类型参数同时写：`struct Box<T: Writer> : Ref { v : T };`

## weak

```
struct Node : Ref {
    parent : weak Node
    child  : Node
    v      : int
};

w := weak(n)
p, ok := w.upgrade()
```

`weak(x)` 的 `x` 必须是 Ref 结构体。

## 类型参数

`<T: Interface>`。使用时显式写实参：`A<SomeType>{}`。

```
struct A<T: Writer> {
    v : T
};

func A.Get<T: Writer>() : T { return this.v }
func A.Set<T: Writer>(a : T) { this.v = a }

a := A<File>{}
a.Set<File>(f)
x := a.Get<File>()
```

```
struct Pair<A: Eq, B: Eq> {
    a : A
    b : B
};
```
