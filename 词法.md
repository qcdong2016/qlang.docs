# 词法

## 关键字

```
package import func type struct interface weak
let if else for switch case default break continue
goto return defer range fallthrough
true false nil
```

`this` 仅在 `func Class.Func` 体内有效。`any` 是预声明空接口。`error` `string` `int` `Ref` `Map` `Type` `StructField` 是预声明名。`make` `len` `cap` `append` `sizeof` `typeof` `panic` `recover` `weak` 是内建。

## 标识符

`XID_Start` + `XID_Continue`，加 `_`。`_` 是空白标识符。

```
let x : int
let _x : int
let foo_bar : int
let 点数 : int
_, err := open("a.q")
```

## 字面量

```
42
0xFF
0b1010
0o755
1_000_000
3.14
1e6
.5
true
false
"hello"
"a\nb"
`原始字符串
可换行，不转义`
'A'
'\n'
'\x41'
```

`char` 与 `uint8` 不是同一类型。单引号一个字节，类型为 `char`。

`nil` 可用于 Ref 结构体、`[]T`、`Map<K, V>`、`func`、`interface`、`weak T`。

`<...>` 在类型位置、以及调用/字面量前是类型实参（`A<T>{}`、`B<T>()`）；比较仍是 `a < b`。

`<` `>` 各是一个 token，不合并 `<<` `>>`。`Map<K, V>`、`A<B<C>>` 每个 `>` 关一层。`<<` `>>` 由相邻两个 `<` 或 `>` 在表达式里合成。
