# 语句

赋值、短声明是语句。表达式语句只能是调用。

## if

条件必须是 `bool`。无括号。必须有 `{}`。

```
if x > 0 {
    return 1
} else {
    return 0
}
if y := x - 1; y > 0 {
    return y
}
```

## for

```
for i := 0; i < 10; i += 1 {
}
for x < 10 {
    x += 1
}
for {
    break
}
for i, v := range a {
}
for k, v := range m {
}
for i, c := range s {
}
```

`range` 数组/切片的 `v` 是拷贝。`range` 字符串按码点，`i` 为字节下标，`c` 为 `int`。不支持 `range` Ref 结构体。

## switch

默认不贯穿。`fallthrough` 进入下一 case。无表达式等于 `switch true`。

```
switch x {
case 1:
    return 1
case 2, 3:
    return 2
default:
    return 0
}
```

```
switch {
case x > 0:
    return 1
default:
    return 0
}
```

```
switch n := len(s); n {
case 0:
    return 0
default:
    return n
}
```

## break / continue / goto

```
outer:
    for i := 0; i < 10; i += 1 {
        if i == 0 {
            continue
        }
        if i == 8 {
            break outer
        }
    }
```

## return / defer

`return` 与返回值必须在同一行。`defer` 后进先出，参数当时求值。

```
return 1, 2
defer f.Close()
```
