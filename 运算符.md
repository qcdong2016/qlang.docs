# 运算符重载

`func T.op(...)`。`this` 是左操作数。

不可重载：一元算符、`=` `:=` `&&` `||` `.` 调用 `()`。

| 算符 | 名称 | 用途 |
| --- | --- | --- |
| `+` | Add | 加法 |
| `-` | Sub | 减法 |
| `*` | Mul | 乘法 |
| `/` | Div | 除法 |
| `%` | Mod | 取模 |
| `&` | And | 按位与 |
| `\|` | Or | 按位或 |
| `^` | Xor | 按位异或 |
| `&^` | AndNot | 按位清除（`a &^ b` ≡ `a & ^b`） |
| `<<` | Shl | 左移 |
| `>>` | Shr | 右移 |
| `==` | Equal | 相等 |
| `!=` | NotEqual | 不等 |
| `<` | Lt | 小于 |
| `<=` | Le | 小于等于 |
| `>` | Gt | 大于 |
| `>=` | Ge | 大于等于 |
| `[]` | Index | 下标读取 |
| `[]=` | IndexSet | 下标写入 |

声明用算符本身作方法名，不用上表英文名。

```
func Point.+(b : Point) : Point {
    return Point{x: this.x + b.x, y: this.y + b.y}
}

c := a + b
```

```
func Point.==(b : Point) : bool {
    return this.x == b.x && this.y == b.y
}

_ = a == b
```

```
func Vec.[](i : int) : int {
    return this.data[i]
}

func Vec.[]=(i : int, v : int) {
    this.data[i] = v
}

x := v[0]
v[0] = 1
```
