# 附录 B. 语法漏洞与优化

对照当前 wiki 全文（含附录 A）的缺口与可收紧点。

---

## 1. 规范自相矛盾 / 笔误

| 问题 | 位置 | 说明 |
| --- | --- | --- |
| 浮点词法 | 附录 A | 已补 `\.[0-9_]+...`，例 `.5` 仍待全文统一 |
| `weak` | 14 / 关键字 | 类型修饰 + 内建 `weak(x)`，未进关键字表 |
| `delete` | 23. 内建 | 有用法，关键字/内建列表未列 |
| `&^=` | 10 / 附录 A | 有 `&^`，复合赋值缺 `&^=` |
| 可见性例 | 6 vs 7 | `math.add`（小写）与 `math.Add`（导出）冲突 |

---

## 2. 语义未闭合（实现必踩坑）

1. **无形式文法**：只有例子 + 词法片段，缺 EBNF；Ref 结构体字面量 `{...}`歧义未写消解规则。
2. **自动分号**：附录 A 一句带过；前缀 `++x`、换行后 `(...)` 调用、`return\n(1,2)` 等未定义。
3. **运算符**：无结合性；缺 `.` `[]` `()` 调用/`{}` 字面量层级；赋值是否为语句未写清。
4. **`<...>`**：`Map<K,V>`、`Box<T>` 与比较运算符 `a < b` 词法冲突未处理。
5. **字符串**：`s[0]` 类型？`range s` 按字节还是码点？编码未写。
6. **可比性**：`float`/`any`/接口/元组/`struct` 能否 `==`、作 `Map` key 未完整表。
7. **包模型**：多文件同包？`init`？`qlang.q`/`Compile` 作用域与可见性？导入路径解析？
8. **RC**：循环只靠 `weak`；值结构体可变方法的隐式传址规则；方法值捕获 `this` 的生命周期。
9. **断言**：`r.(Node)` 单值 panic / 双值 ok / 元组 `.0/.1` 三种并存，优先规则未写。
10. **泛型**：仅结构体类型参数；无泛型函数/方法/接口；约束只能是接口名。
11. **缺省能力（若故意没有应在首页写死）**：裸指针、`&` 类型前缀、`const`/`iota`、结构体内嵌、命名返回值、并发语法。

---

## 3. 建议优化（按收益）

1. **补附录 C：EBNF**（声明 / 类型 / 表达式 / 语句），把 `<...>`、复合字面量消歧写死。
2. **`weak` 升为关键字**；内建表补 `delete`/`weak`。
3. **优先级表补全** + 左右结合；补 `&^=`；考虑只保留后缀 `++/--`，去掉前缀以免与一元 `+` 和 ASI 纠缠。
4. **首页「明确不做」清单**：裸指针、`&` 类型前缀、结构体内嵌、泛型函数、三元、多返回值（非元组）等，避免读者当漏洞。
5. **字符串与 `range`**：写明 UTF-8、索引/`len` 按字节；若要字符迭代另给 API。
6. **`<` 歧义**：类型实参改用 `Box[T]` / `Map[K,V]`（Go 风格）或强制 `Map::<K,V>`。

---

## 4. 优先级建议

| 级 | 项 |
| --- | --- |
| P0 | `weak`/`delete` 归类、`<` 冲突 |
| P1 | EBNF、ASI、运算符表、字符串/`range`、可比性表、包/多文件 |
| P2 | 首页「不做」清单、前缀自增删减 |
