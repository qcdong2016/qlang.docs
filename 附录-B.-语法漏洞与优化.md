# 附录 B. 语法漏洞与优化

对照当前规范全文（含附录 A）的缺口与可收紧点。

---

## 1. 规范自相矛盾 / 笔误

| 问题 | 位置 | 说明 |
| --- | --- | --- |
| 浮点词法 | 附录 A | 已补 `\.[0-9_]+...`，例 `.5` 仍待全文统一 |
| `weak` | 3 / 14 | 已升为关键字；内建 `weak(x)` 见 23 |
| `&^=` | 10 / 附录 A | 有 `&^`，复合赋值缺 `&^=` |
| 可见性例 | 6 vs 7 | `math.add`（小写）与 `math.Add`（导出）冲突 |

---

## 2. 语义未闭合（实现必踩坑）

1. **无形式文法**：只有例子 + 词法片段，缺 EBNF；Ref 结构体字面量 `{...}`歧义未写消解规则。
2. **自动分号**：换行后 `(...)` 调用等边界未完整列举。
3. **运算符**：无结合性；缺 `.` `[]` `()` 调用/`{}` 字面量层级；赋值是否为语句未写清。
4. **字符串**：已定为 UTF-8；`len`/`[]` 按字节，`range` 按码点。
5. **可比性**：已定为实现 `Eq` 即可 `==`/`!=` 与作 Map key；有序比较范围仍可再收紧。
6. **包模型**：多文件同包？`init`？`qlang.q`/`Compile` 作用域与可见性？导入路径解析？
7. **RC**：循环只靠 `weak`；值结构体可变方法的隐式传址规则；方法值捕获 `this` 的生命周期。
8. **泛型**：仅结构体类型参数；无泛型函数/方法/接口；约束只能是接口名。
9. **缺省能力（若故意没有应在首页写死）**：裸指针、`&` 类型前缀、`const`/`iota`、结构体内嵌、命名返回值、元组、自增运算符、并发语法。

---

## 3. 建议优化（按收益）

1. **补附录 C：EBNF**（声明 / 类型 / 表达式 / 语句），把复合字面量消歧写死。
2. **`weak` / `typeof` 已入内建**；类型断言为 `typeof(x) == T`。
3. **优先级表补全** + 左右结合；补 `&^=`。
4. **首页「明确不做」清单**：裸指针、`&` 类型前缀、结构体内嵌、泛型函数、三元、元组、自增运算符等。
5. **类型实参 `<...>`**：在类型位置（如 `Map<K,V>`、`Box<T Writer>`）解析为类型实参列表，在表达式位置为比较运算符，由语法上下文区分。

---

## 4. 优先级建议

| 级 | 项 |
| --- | --- |
| P0 | （已处理）`weak`/`typeof`/类型断言 |
| P1 | EBNF、ASI、运算符表、包/多文件 |
| P2 | 首页「不做」清单 |
