# 附录-词法

空白：空格、tab、换行。

数字：`[0-9_]+`、`0x[0-9a-fA-F_]+`、`0b[01_]+`、`0o[0-7_]+`、`[0-9_]+\.[0-9_]*([eE][+-]?[0-9_]+)?`、`\.[0-9_]+([eE][+-]?[0-9_]+)?`。

标识符：`XID_Start` + `XID_Continue`，加 `_`。

```
+ - * / % & | ^ &^ << >>
+= -= *= /= %= &= |= ^= &^= <<= >>=
== != < <= > >=
&& || !
= :=
.
( ) [ ] { }
, : ; ...
```

无前缀 `*` `&`。`struct T : Ref` 的类型名仍是 `T`。`func Class.Func` 的 `.` 是方法声明。类型名单独作表达式时类型为 `Type`；`Struct.Field` 类型为 `StructField`。

## 换行结束语句

下列 token 之后换行，视为语句结束：标识符、整数、浮点、字符、字符串、`break` `continue` `fallthrough` `return` `true` `false` `nil` `this`、`)` `]` `}`。

因此 `f` 换行再 `(x)` 是两条语句，不是调用；`x` 换行再 `.y` 也不是选择。`return` 后换行即结束，返回值必须同行。

类型与复合字面量的 `{` 之间不插结束：`Point{1, 2}`、`Box<File>{v: f}` 必须连写（`>` 与 `{` 可同行紧挨）。
